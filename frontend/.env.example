# 默认使用相对路径 /api，并由 Vite proxy 或部署网关转发到后端。
VITE_API_BASE_URL=

# 仅 Vite 开发代理使用；浏览器端不会读取该变量。
ODDS_API_PROXY_TARGET=http://127.0.0.1:8013

# 仅本地开发需要保留页面结构时开启；生产环境保持 false。
VITE_ENABLE_MOCK_FALLBACK=false

# 管理员登录由后端 HttpOnly Cookie 处理，前端不要配置账号、密码或密钥。
# 如果前后端跨域部署，后端 CORS_ALLOW_ORIGINS 需要包含当前前端域名。
